/*import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CompartirPerfilPage } from './compartir-perfil.page';

describe('CompartirPerfilPage', () => {
  let component: CompartirPerfilPage;
  let fixture: ComponentFixture<CompartirPerfilPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CompartirPerfilPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
